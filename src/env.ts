// SVG stuff
export const NONE = 'none';

// Length units for CSS
export enum Unit {
    percent = '%',
    px = 'px',
    em = 'em',
    rem = 'rem',
    in = 'in',
    vh = 'vh',
    vw = 'vw'
}

// Defaults (not theming)
export const DEFAULT_COLOR = 'orange';
export const DEFAULT_BORDER_WIDTH = 0;
export const DEFAULT_UNIT = Unit.px;
export const DEFAULT_FILL = true;
export const DEFAULT_GRAPH_CONTENT_WIDTH = '90%';
export const DEFAULT_GRAPH_CONTENT_HEIGHT = '90%';
